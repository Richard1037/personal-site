# 统一换行符，避免 Windows / macOS / Linux 之间协作时整文件 diff 噪音。
# 仓库内一律存 LF，检出也一律 LF。
* text=auto eol=lf

# 二进制文件不要做文本转换
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.webp binary
*.ico binary
*.woff binary
*.woff2 binary
